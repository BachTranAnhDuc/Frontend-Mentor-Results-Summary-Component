import React, { FC, ReactElement } from 'react';

const App: FC = (): ReactElement => {
  return (
    <section className="h-screen bg-neutral-pale-blue">This is react</section>
  );
};

export default App;
